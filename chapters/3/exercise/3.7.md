```
long scale2(long x, long y, long z) {
  long t = x + 4x + 2y + 8z
  return t;
}
用GCC编译实际的函数得到如下的汇编代码：
long scale2(long x, long y, long z)
x in %rdi,y in %rsi, z in %rdx
scale2:
  leaq (%rdi,%rdi,4), %rax
  leaq (%rax,%rsi,2), %rax
  leaq (%rax,%rdx,8), %rax
  ret
填写出C代码中缺失的表达式。
```
